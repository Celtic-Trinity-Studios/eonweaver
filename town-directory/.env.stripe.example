# Copy to .env.stripe (gitignored). Never commit real keys.
#
# Stripe Dashboard → Developers → API keys → Secret key
#   sk_test_…  — safe trial run (test cards)
#   sk_live_…  — production billing on eonweaver.com
#
STRIPE_SECRET_KEY=sk_test_or_sk_live_paste_here
