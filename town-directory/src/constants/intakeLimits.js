/**
 * Max arrivals for AI intake (roster + flesh) and forced sim intake per request.
 * Keep in sync with PHP clamps in intake_actions.php and sim_run.php.
 */
export const MAX_INTAKE_ARRIVALS = 150;
